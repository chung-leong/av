// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("av", "for av support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("av", "enable av support", "no");

if (PHP_AV != "no") {
	EXTENSION("av", "av.c");
}

